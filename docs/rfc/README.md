# HomeFleet Protocol (HFP) — RFC drafts

The versioned protocol specification lives here. `hfp-v0.md` lands with milestone M1 (protocol package).

The protocol covers: node identity & capabilities exchange (`hello`), job delegation, event streaming, results, and cancellation — designed so future job types (e.g. model-pool orchestration) extend it without redesign.
